#include <stdio.h>
int main(void)
{
	int x, n;
	scanf("%d%d", &x, &n);
	if (n == 1)
	{
		x = x >> (8*n);
	}
	else if (n == 2)
	{
		x = x >> (8*n);
	}
	else
	{
		x = x >> (8*n);
	}
	printf("%d", x);






	return 0;
}
